[Unit]
Description=vpn container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a vpn
ExecStop=/usr/bin/docker stop -t 2 vpn

[Install]
WantedBy=multi-user.target
